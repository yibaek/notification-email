export namespace EmailEnum {
  export enum Lang {
    KO = 'ko-KR',
    EN = 'en-US',
  }

  export enum Env {
    DEV = 'dev',
    TEST = 'test',
    PRODUCTION = 'prod',
  }

  export enum MessageType {
    TEMPLATE = 'template',
    HTML = 'html',
  }
}
